diff a/sales/udins/service_remarks_build.py b/sales/udins/service_remarks_build.py	(rejected hunks)
@@ -63,22 +63,18 @@ def find_client_by_code_in_remarks(remarks: str) -> Client | None:
             ):
                 score = 300 + len(code)
 
-            if score < 0:
-                continue
-            if score > best_score or (
-                    score == best_score
-                    and best is not None
-                    and len(code) > len((best.client_code or "").strip())
-            ):
-                best_score = score
-                best = row
+        if score < 0:
+            continue
 
-        if best is None:
-            prefix = text_upper[:4].strip()
-            if prefix:
-                best = Client.objects.filter(client_code__iexact=prefix).first()
+        if score > best_score or (
+                score == best_score
+                and best is not None
+                and len(code) > len((best.client_code or "").strip())
+        ):
+            best_score = score
+            best = row
 
-        return best
+    return best
 
 
 def resolve_client_for_remarks(*, remarks: str, client) -> Client | None:
