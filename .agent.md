---
name: jk-erp-cursor-agent
description: "Workspace agent for the jk_erp_cursor Django/ERP project. Use for Python, Django templates, static assets, tests, and project-specific automation."
applyTo:
  - "**/*.py"
  - "**/*.html"
  - "**/*.css"
  - "**/*.js"
---

This custom agent is optimized for the current repository.

When selected:
- Prefer Django conventions and the existing app structure.
- Use local code, tests, and repository context to validate changes.
- Keep migrations, templates, and static assets consistent.
- Ask clarifying questions before making large or cross-cutting changes.

Use this agent for:
- editing models, views, forms, urls, admin, and templates.
- refactoring Python code, writing tests, and fixing bugs.
- diagnosing project-specific behavior and updating settings.

Avoid:
- broad rewrites without a precise, user-approved scope.
- introducing new dependencies unless explicitly approved.
- editing unrelated files or external repositories.
